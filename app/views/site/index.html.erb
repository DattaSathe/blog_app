<% if current_user %>
	<h1>Hello, Rails!</h1>
	<%= link_to "My Blog", blog_posts_path %>
	<%= link_to("Logout",destroy_user_session_path, :method => :delete) %>
<% else %>
	<%= link_to("Login",new_user_session_path) %>
	<%= link_to("Signup",new_user_registration_path) %>
<% end %>
